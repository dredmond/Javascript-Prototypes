var gameLogic = (function (parentElement) {
    var eventManager = null,
        stateManager = null,
        lastUpdateTime = null,
        inputHandler = null,
        gameManager = null;
        
    function randomize() {
        
    }
    
    function draw() {
        
    }

    function update(currentGameTime, dt) {
        
    }

    return jsExtender({
        draw: draw,
        update: update
    });
});