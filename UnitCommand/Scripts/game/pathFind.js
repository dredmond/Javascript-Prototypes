/*
 * Fresh start on the path finding algorithm. 
 * The last one was getting messy and confusing.
 * Hopefully this one will be successful. =)
 *
 */
var pathFind = pathFind || (function (mapData) {
    function tileInfo() {
        return {

        };
    }

    function execute() {
        
    }

    return {
        tileInfo: tileInfo,
        execute: execute
    };
});